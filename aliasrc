alias ls="lsd"
alias l="lsd"
alias la="lsd -aA"
alias lrth="lsd -lrtha"
alias h="history | fzf -e --tac"
alias p="ping"
alias pg="ping www.google.com"
alias p1="ping 1.1"
alias c="clear"
alias y="yarn"
alias du="du -h"
#alias code="code-insiders"
#alias vs="code-insiders"
#alias open="xdg-open" # Remove for MacOS
alias o="open"
alias py="python3"
alias v="nvim"
alias dc="docker-compose"
alias dcu="docker-compose up"
alias dcd="docker-compose down"
alias dcr="docker-compose restart"
alias du="dust" # Graphical du replacement. Easily see how disk is being used
alias b="bat"
# alias fzf="fzf -m"
# alias f="fzf"
alias t="tmux"
alias y="yarn"
alias logview="less +F"
alias n="node"
alias d="delta" # Diff replacement
alias diff="difft" # Diff replacement
alias df="duf" # df replacement

# VS Code
alias code="code-insiders"
alias vs="code"

# Xclip (copy to clipboard from terminal)
alias xclip="xclip -sel clip"
alias xc="xclip"

# Clip command to copy from WSL to Windows clipboard
alias clip="clip.exe"

# Git aliases
alias gcam="git commit -am"
alias gc="git commit"
alias gcm="git commit -m"
alias gaa="git add --all"
alias gco="git checkout"
alias g="git"
alias gpu="git push"
alias gpl="git pull"
alias gl="git log --all --graph --decorate"
alias gh="git log --all --graph --decorate --oneline"
alias gbr="git branch -vv"
alias grbi="git rebase --interactive"
alias gap="git add -p"

# Helpers
alias mv="mv -i"
alias cp="cp -i"
alias j="z"
alias e="explorer.exe"
alias whatip="curl ipecho.net/plain"

alias pn="pnpm"

# Warp Terminal aliases
alias ..="cd .." # Fixes ...,.... autocomplete coming in above ../ directory

# Quasar
alias qd="quasar dev"
alias qb="quasar build"
alias qs="quasar serve"

# Flutter
alias flutter="fvm flutter"
